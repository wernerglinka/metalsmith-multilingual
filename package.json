{
  "name": "metalsmith-multilingual",
  "version": "0.0.2",
  "description": "A Metalsmith plugin that adds internationalization metadata to files based on directory structure and frontmatter alternate links",
  "type": "module",
  "main": "./src/index.js",
  "exports": "./src/index.js",
  "engines": {
    "node": ">= 22.0.0"
  },
  "scripts": {
    "changelog": "auto-changelog -u --commit-limit false --ignore-commit-pattern '^((dev|chore|ci):|Release)'",
    "coverage": "./scripts/test.sh --test-reporter=lcov --test-reporter-destination=coverage/lcov.info --test-reporter=spec --test-reporter-destination=stdout",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome check --write .",
    "lint:check": "biome check .",
    "release:patch": "./scripts/release.sh patch --ci",
    "release:minor": "./scripts/release.sh minor --ci",
    "release:major": "./scripts/release.sh major --ci",
    "release:check": "npm run lint:check && release-it --dry-run",
    "test": "./scripts/test.sh",
    "test:coverage": "./scripts/test.sh --test-reporter=lcov --test-reporter-destination=coverage/lcov.info --test-reporter=spec --test-reporter-destination=stdout",
    "test:unit": "node --test test/unit/dot-path.test.js test/unit/locale-detector.test.js test/unit/hreflang-builder.test.js",
    "test:esm": "node --test test/index.test.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wernerglinka/metalsmith-multilingual"
  },
  "keywords": [
    "metalsmith",
    "metalsmith-plugin",
    "i18n",
    "internationalization",
    "localization",
    "hreflang",
    "multilingual"
  ],
  "files": [
    "src",
    "LICENSE",
    "README.md"
  ],
  "author": "Werner Glinka <werner@glinka.co>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/wernerglinka/metalsmith-multilingual/issues"
  },
  "homepage": "https://github.com/wernerglinka/metalsmith-multilingual",
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "auto-changelog": "^2.5.0",
    "metalsmith": "^2.6.3",
    "release-it": "^19.0.2"
  },
  "peerDependencies": {
    "metalsmith": "^2.5.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
